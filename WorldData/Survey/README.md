# Survey Data

Store only source metadata and redistribution-safe derived records in Git.

Raw point clouds, scan meshes, plans, and restricted photographs must be kept in `WorldData/Survey/Restricted` or approved external storage. That directory is ignored. Do not add survey material until written terms permit the intended use.
